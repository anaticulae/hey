# Test resources

There are two different types of script generators. The `before.sh` and
`after.sh` script. Before ensure the preconditions for running the project test
suite. The after.sh script delivers data for further applications which uses
the data output of these project.
