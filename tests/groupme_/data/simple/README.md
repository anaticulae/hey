# Example - simple

This content is generated due:

```[bash]
rm rawmaker__*
rawmaker -i howto_pyporting.pdf -o . --text --toc --border
```

## Features

### Footer extraction

Hint: This footer is not separated by a line, but there is a footer
      containing the page number.
